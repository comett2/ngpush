import { NgDetectionStrategy } from './ng-detection-strategy';

describe('Ngdetectionstrategy', () => {
  it('should create an instance', () => {
    expect(new NgDetectionStrategy()).toBeTruthy();
  });
});
